// Axtra Venture - Custom Script

document.addEventListener('DOMContentLoaded', () => {
    console.log("Axtra Venture site loaded.");
    
    // Smooth scrolling for anchor links is handled by CSS (scroll-behavior: smooth)
    // Alpine.js handles the mobile menu and FAQ accordion logic.
});
